<h2>{{#if page.section }}{{ section }}{{else}}{{ category }}{{/if}}</h2>

{{#with (get-nav-slice nav-new this 0 site.baseurl) }}
  <div class="accordion">
    <div class="expandable">
      <ul>
        {{#each section }}
          {{#if section }}
            <li class="js-expandable{{#if current}} js-open{{/if }}">
              <h6>{{ title }}</h6>
              <ul>
              {{#each section }}
                <li><a href="{{ url-maker url 'jump' }}">{{ title }}</a></li>
              {{/each }}
              </ul>
            </li>
          {{ else }}
            <li><a href="{{ url-maker url 'regular' }}"><h6>{{ title }}</h6></a></li>
          {{/if }}
        {{/each }}
      </ul>
    </div>
  </div>
{{/with }}
